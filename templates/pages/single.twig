{% extends 'layouts/base.twig' %}
{% block content %}
    <divv class="container">
        <div class="columns">
            <div class="column">
                <h1 class="title is-1">{{ title }}</h1>
                {{ post.content }}
            </div>
        </div>
    </divv>
{% endblock %}