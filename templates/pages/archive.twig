{% extends 'layouts/base.twig' %}
{% block content %}
    <div class="container">
        <div class="columns">
            <div class="column">
                <h1 class="title is-1">{{ title }}</h1>
            </div>
        </div>
        <div class="columns is-multiline">
            {% for post in posts %}
                <div class="column is-full">
                    {% include ['partials/teaser-'~post.type.slug~'.twig', 'partials/teaser.twig'] %}
                </div>
            {% else %}
                {% include 'partials/nothing.twig' %}
            {% endfor %}
        </div>
        {% if posts %}
            <div class="columns">
                <div class="column is-full">
                    {% include 'partials/pagination.twig' with {posts: posts} only %}
                </div>
            </div>
        {% endif %}
    </div>
{% endblock %}